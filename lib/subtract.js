
/**
 * @description takes two arguments of type number with first argument as the minuend and second agurment the subtrahend and return first argument minus second argument
 * @param {*} num1 minuend 
 * @param {*} num2 subtrahend
 * @returns 
 */
const subtract = (num1,num2)=>{
    return num1 - num2
}

export default subtract;