Please add a documenting string at the top of each method for example:

/**
 * @name Sum
 * @description takes in an array as an argument, map through the array and add the value of each element to the sum.
 * @param {*} array 
 * @returns : {Number} sum
 */